# 运营统计看板与历史工时分析实施报告

## 结果

- 已按模板和稳定节点生成处理、审核并列柱状图，并提供日、周、月双趋势图。
- 已提供模板版本、业务状态、业务、稳定节点、实际处理人和实际审核人筛选；版本默认合并。
- 已提供模板级业务完成、节点处理合计和审核合计统计。
- 明细下钻提供有效样本、精确中位数、平均/最短/最长、待日历补算、历史未记录、分页业务、处理/审核轮次和实际投票响应。
- 所有活动账号可查看不含业务明细的全局汇总；普通账号下钻逐条执行当前账号和业务关系复核，超级管理员可查看全部明细并保留旧当前指标与 CSV。
- 已新增 `operationsAnalytics`，以确定性事实、每日汇总、固定批次、持久游标和可信 Timer 生成统计数据；业务流转不依赖统计成功。
- 已更新集合、索引、空触发器、一次性 Timer 和多账号隔离验收手册。

## TDD 证据

- 领域规则、工作器、事实完整性、每日汇总、受保护路由、筛选、汇总、权限下钻、客户端图表和分页均先取得预期 RED，再完成最小 GREEN。
- 最终全量：`businessApi` 558/558、`operationsAnalytics` 26/26、`calendarSync` 55/55、`workflowReminder` 30/30、`evidenceRetention` 42/42、小程序 162/162、WXML 4/4，全部 0 失败。
- 生产 JavaScript 语法、`git diff --check` 与项目记忆校验均通过。
- 最终自审新增“普通账号业务筛选项返回前重新鉴权”回归：RED 为仓储聚焦 9/10，最小修复后 10/10。
- 最终展示自审新增“合法零分钟与历史未记录明确区分”回归：RED 为客户端聚焦 8/9，最小修复后 9/9。

## 提交

- `d8c4c0a`：运营统计领域规则。
- `fb18e67`：业务和节点统计待生成状态。
- `79ca20d`：独立可信统计工作器。
- `8bcc38e`：确定性事实与每日汇总。
- `7384496`：受保护历史统计接口、原生图表、筛选器与权限下钻。
- `867072b`：待日历补算事实刷新、部署索引契约与安全边界回归。
- 部署手册、项目记忆与本报告由最终交付证据提交纳入。

## 未验证边界

- 真实 CloudBase 新集合、组合索引、查询选择、事务竞争和 `operationsAnalytics` 部署未验证。
- `operationsAnalytics` 真实一次性 Timer、正式 15 分钟触发器和空触发器回滚未验证；正式触发器不得自动启用。
- 微信开发者工具视觉、真机、多账号全局汇总与权限下钻未验证。
- 真实历史数据的首次物化、范围超过 2,000 条时的收窄提示和完整日历覆盖未验证。
