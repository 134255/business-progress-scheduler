# B1 完整导出读取去重验收

日期：2026-09-24。下文记录本地实现、完整自动回归及一次最终独立复核阶段，当时尚未发布或提交。后续用户授权的发布已完成，见末尾发布记录及项目 STATUS；实施阶段的限制和未验证项仍适用。

## 本批边界

用户批准在原脏目录连续开发、最后统一复核。启动检查点覆盖 miniprogram、cloudfunctions（排除依赖）及 project.config.json 的433个文件，原有已发布未提交功能全部保留。

运行时增量仅两个仓储：

- `cloudfunctions/businessApi/lib/cloud-operations-field-repository.js`：每次完整导出单独记录成功核验的来源文档地址；尾部省重复预读，仍重新扫描审核票、逐个权威事务读取来源和最后重新鉴权。地址不进入结果、摘要、游标或报告缓存。
- `cloudfunctions/businessApi/lib/cloud-operations-repository.js`：仅完整基础报告不查询未使用的待审核列表；普通仪表和旧导出默认路径保持。

其余代码仅增加一个后端投影测试、离线探针及其97项测试、固定合成基线；A工具一项测试预算从3000调为2000（省读后3000已能完成两页，旧“一页”断言确实失败，修改后仍验证第二页中止）。**CLI默认20000读、30秒、256MiB预算未改**。

433文件核对：变化恰为上述两个运行时模块及 `cloud-operations-repository.test.js`；新增/删除均0。小程序页面、服务、配置、依赖、其他云函数完全未改。无新集合、索引、Timer、通知、云端业务读写或真实文件访问。暂存区保持空。

## 实测读取量

本地合成数据、真实仓储/域逻辑、只读数据库适配器；不是云端账单、延迟或并发人数测量。旧版基线在改产品前生成并冻结，附原文件SHA256；有序完整行摘要不排序、不去重，游标仅保存纯合成解密体，不记录密钥/密文/客户信息。

| 单节点样本 | v1/v2完整行数 | 首/暖/冷页改前读数 | 改后读数 | 每页事务数 | 最大事务操作 |
| --- | --- | ---: | ---: | ---: | ---: |
| 无审核 | 13 / 11 | 29 | 26 | 6（不变） | 4（不变） |
| 或签 | 13 / 11 | 37 | 33 | 6（不变） | 6（不变） |
| 会签 | 13 / 11 | 39 | 35 | 6（不变） | 7（不变） |
| 2495组合严格联动（v2） | 9 | 29 | 26 | 6（不变） | 4（不变） |

7组完整报告的有序行摘要、行数和首个游标体全部与冻结旧版一致；旧游标可在新冷实例继续，账号/查询绑定和20分钟到期仍生效。缓存淘汰后可以重建。9组非导出摘要/筛选/分析的响应、完整读取轨迹及指标均与改前一致。

独立售后、每条一个完成节点的扩大样本（每个首/暖/冷页分别测量，三种分页指标相同；未遍历150节点全部报告）：

| 节点数 | 审核模式 | 每页读数 | 返回文档次数 | 事务数 | 最大事务操作 | 活跃读峰值 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 无审核 / 或签 / 会签 | 26 / 33 / 35 | 25 / 32 / 36 | 6 | 4 / 6 / 7 | 1 |
| 10 | 无审核 / 或签 / 会签 | 143 / 213 / 233 | 178 / 248 / 288 | 42 | 4 / 6 / 7 | 4 |
| 150 | 无审核 / 或签 / 会签 | 1986 / 3036 / 3336 | 2558 / 3608 / 4208 | 602 | 4 / 6 / 7 | 4 |

上表150条售后不能与下面A模型30条售后×5节点混为同一个样本；少掉的是无审核每节点2次、有审核每节点3次重复文档读取，再加一项无关待审核查询，核验事务没有减少。所有只读样本写入尝试均0。

### A样本前后同预算对照

30条售后×5个无审核完成节点，150节点，50行/页；v1和v2相同读取量：

| 指标 | 改前 | 改后 |
| --- | ---: | ---: |
| 首个/最后一个完整页逻辑读取 | 1789 | 1488 |
| 20000读预算内完整页数 | 11 | 13 |
| 预算内返回行数 | 550 | 650 |
| 累计读取/返回文档次数 | 20000 / 24013 | 20000 / 24724 |
| 累计事务数（包含下一未完成页） | 4046 | 4838 |
| 最大事务操作 / 活跃读峰值 | 4 / 4 | 4 / 4 |
| outcome / complete / 退出码 | test_aborted / false / 1 | test_aborted / false / 1 |

每页减少301读，约16.8%。相同预算可前进更多页，故累计事务变多；不是减少权威核验。**整份报告仍未完成**，不能把650行当总行数或承诺线上导出成功。关联2495组合综合校验从172读降到160读、34事务不变；这含多个接口请求，不是单页成本。

完整smoke 33场景：30个ok、2个报告预算中止、1个故意缺来源incomplete，退出1；未知error为0，写入尝试为0。其他场景计数/读取与A一致。

## 验证证据

环境：Node.js v24.14.0。新鲜运行、独立退出码检查，除已标明测量中止外，0失败/取消/跳过。

| 命令 | 结果 |
| --- | --- |
| 旧字段仓储/分析/报告三套定向基线 | 31/31，exit 0 |
| 改前A工具全套 | 39/39，exit 0；加入探针后40/40 |
| 新探针TDD | 模块缺失失败→1/1；6个省读断言失败→7/7 |
| 投影TDD | 待审核查询2而非0失败→25/25仓储测试 |
| 字段来源/域/游标与竞争定向回归 | 171/171，exit 0 |
| `node --test tools/capacity/test/*.test.cjs` | 136/136，exit 0，含新增97项 |
| `npm.cmd test --prefix cloudfunctions/businessApi` | 1423/1423，exit 0 |
| `npm.cmd test --prefix cloudfunctions/operationsAnalytics` | 66/66，exit 0 |
| `node --test miniprogram/test/*.test.js` | 648/648，exit 0 |
| `node tools/test-business-card-rendering.mjs` | 官方WXML 10/10，exit 0 |
| `node tools/test-previous-node-records-rendering.mjs` | 官方WXML 6/6，exit 0 |
| `node tools/test-operations-field-analysis-rendering.mjs` | 官方WXML 8/8，exit 0 |
| `node tools/test-wxml-structure.mjs` | 4/4，exit 0 |
| `git -c core.safecrlf=false diff --check` | exit 0 |
| 项目记忆validator | 更新文档后再次通过，exit 0 |

渲染脚本仅本次进程设置已安装官方 `WECHAT_WCC_PATH`，没有安装编译器或修改项目配置。npm存在原有本机prefix/cache配置警告，测试exit 0；本批未改个人npm配置。

功能覆盖包括：

- 反馈值、相同值更换反馈ID、审核轮次更换/删除、节点删除、审核票新增/移除/替换/拒绝：在首请求或暖续页基础读取阶段发生时，尾部拒绝旧报告。
- 51票超限、账号停用/降权、最终重新授权前撤权、未知读取错误：不放宽、不吞错。
- 基础工时/候选售后或节点增删/历史显示名变化：旧清单失效；无关待审核集合查询故障不会阻断完整报告，相关最终审核轮次读取故障仍失败，旧仪表/导出保持原故障路径。
- 50001行及超12MiB拒绝，正好50000行允许；空结果无伪续页；跨月创建/完成日期保留各自口径。
- 原测试继续验证新增及未匹配分析候选、严格联动、单/多选、0、false、空白、隐藏属性、CSV公式保护、切账号/筛选/取消及两阶段发送。

## 前端功能保护与未验证范围

| 功能 | 本地证据 | 五端原生 |
| --- | --- | --- |
| 登录、切账号、模板编辑/复制/启用与创建售后 | 后端+客户端完整回归 | unverified |
| 分类/品牌/型号/属性联动、智能填充及草稿 | 域/客户端完整回归+2495组合对照 | unverified |
| 图片/视频/PDF选择、3路上传、串行登记、重试、预览 | 既有后端+客户端完整回归，代码未改 | unverified |
| 审核/驳回/返工/意见/历史 | 后端+客户端完整回归及新增来源竞争 | unverified |
| 前序节点、卡片、检索与分页 | 完整回归+官方渲染+原容量场景 | unverified |
| 工时/字段/关联分析、CSV生成/发送/取消 | 完整回归+worker+官方渲染+冻结旧报告 | unverified |
| 分享及附件授权访问 | 既有后端+客户端完整回归 | unverified |

五端指Android、HarmonyOS、iOS、macOS、Windows，均未做本批原生设备验收。本地自动回归未发现功能回退；不能推导成所有设备零风险。若实测有额外功能影响，暂停受影响改动，先向用户说明并批准；本批唯一已披露影响是完整报告不再受无关待审核列表查询故障阻断。

2000候选节点、5000候选售后、10000扫描节点、50000行/12MiB、最多3份合计12MiB进程报告缓存均不变；没有解决月度4500完成节点超限、长期历史扫描和媒体增长。120MiB每轮附件和60天保留不变。真实CloudBase性能/事务/索引/计费、多端使用、备份恢复与提醒上线均unverified。

## 最终复核与交付门槛

一次新鲜独立只读复核通过，无Critical/Important/Minor发现，无遗留小项，无需修正轮次或第二次复核。复核者独立运行153项定向测试和1项预算测试（154/154，0失败/跳过），从启动副本复现全部7个旧报告及9个非导出基线，并核对433文件边界。以启动文件副本而非Git HEAD审查本批增量；副本行尾规范化差异已核对，不能将行尾变换误记为产品逻辑变化。当前未做任何发布、上传或Git集成。

执行裁定（含复核者明确未评判部分及判断错误的代价）：

1. 用文件检查点替代提交范围、手工维护账本，遵守用户原脏目录/不自动提交要求。若边界选错可能覆盖旧改动，故以433摘要及局部副本双重核对，保留检查点不清理。
2. 只调整离线“一页后中止”测试预算，不改CLI预算；若判断错误可能掩盖容量失败，因此相同20000读测量仍如实记录不完整。
3. 既存脏目录功能不做全面代码审计，仅保护不被本批改动并做完整回归；旧功能潜在问题仍可能存在，不声称B1已修复。
4. 真实CloudBase一致性、重试、索引、并发、耗时、配额、费用和生产数据留待授权检查；本地收益可能不等同线上可靠性或费用收益。
5. 五端原生交互及真实CSV发送未验收，官方WXML由主任务运行但不代替设备；可能仍有设备特定问题，不能承诺零影响。
6. 保留现有逐来源核验，不新增整份报告单快照原子性；现有跨扫描/事务并发窗口仍可能存在。
7. 大报告边界、首页调度、媒体增长、保留与恢复另行优化；历史范围导出和运营存储风险尚未消除。
8. 部署准备与最终文档由主任务收尾，复核不等于上线批准；发布前另核对准确载荷，避免脏目录无关变更混入发布。

回退只能撤销本批两个运行时模块的独立增量，保留此前功能及A诊断。原目录与ignored检查点保留，不自动清理或回退整个工作区。若后续授权发布，须再确认部署包精确载荷；本批无客户端产品变化，不需要仅为B1重新上传小程序。

## 后续授权发布（2026-09-24）

用户确认“全部执行”后，使用官方CLI发布businessApi，6498文件、9.0MB。独立下载回读6498/6498 SHA256一致，零增删/差异；应用76文件与本地一致，相对发布前仅两个B1仓储变化，依赖/package原样保留。清单摘要：`9b23b2ca785dc9bbe71ffbd3164be35b62a76e7e02a7ed91dfd844d566ffbcb7`。

实际载荷独立QA重新通过1423/1423，客户端648/648、工具136/136、worker66/66、官方WXML10/6/8、结构4均通过。未修改业务记录、权限、资源、索引、环境变量、Timer或其他云函数。

同时按用户要求上传小程序1.2.8，包1397367字节；客户端增量是A的默认关闭诊断动作，并非B1页面改动。独立上传目录逐文件核验，不含云函数、用户附件或部署输出。仅上传开发版本，不等于体验版切换、微信审核或正式发布。Git分批集成结果见STATUS。五端原生和真实云端CSV使用仍unverified。
