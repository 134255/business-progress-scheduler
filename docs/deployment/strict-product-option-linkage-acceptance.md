# 严格商品联动发布与验收

状态：核心、编辑器和识别兼容复审全部批准；三个相关函数已部署回读，小程序1.2.1上传成功。用户当次确认后，正常编辑流程已替换旧SKU并保存整个模板为停用版本6，云端完整组合与保留配置回读通过。随后用户另行要求启用，正常模板管理操作成功，刷新列表显示已启用版本7、10节点；真机体验仍未验收，当前证据以 STATUS 为准。

## 发布边界

- 使用当前完整工作区，保留此前已上线但未提交的修复；不能用 Git HEAD 覆盖生产。
- 本功能涉及 businessApi/lib、operationsAnalytics/lib 与识别依赖 nodeTextParser/lib/parser-domain.js；三个入口及包文件已与线上下载副本哈希核对一致。识别候选从100放宽至5000须两端匹配且共守256KiB规范化schema预算；只部署API的101/2495候选互通测试失败，不能遗漏解析器。线上没有本地缺失的额外lib文件。
- 使用官方开发者工具 `cloud functions download` 留存发布前代码，`inc-deploy --file lib` 只更新代码；不得套用本地空触发器配置。更新后再次下载逐文件比对。
- 保持原256MB/60秒、环境变量、权限；businessApi无Timer，operationsAnalytics保留 `operations-analytics-every-15-min` / `0 */15 * * * * *`。不调用伪造Timer，不创建生产测试售后。
- 客户端需上传新版；上传成功不是提交审核/正式发布或用户手机已更新的证据。

## 已核验进展

- operationsAnalytics：官方CLI `inc-deploy --file lib`成功（10库文件）；发布后下载13/13生产与包文件哈希一致。控制台更新时间2026-09-18 16:47:12，正常；256MB、60秒、原Timer及环境/网络配置逐项保持。
- nodeTextParser单文件增量发布后7/7生产及包文件回读一致；businessApi/lib增量发布68文件后71/71一致。两函数正常、256MB/60秒、环境/网络配置不变，无Timer，更新时间分别17:07:42及17:08:10。
- 官方CLI上传小程序1.2.1成功，1207849字节；没有提交审核或正式发布，没有真机更新/公众平台体验版指向证据。
- 实际导入：源文件摘要未变；开发者工具普通编辑页显示校验通过2495组合、229型号组，字典数量4/47/229/543/396/172/39/7。只读界面已逐级展开分类/品牌/型号/属性1；完整四类原生试选仍待验收。用户当次确认后已应用、保存17字段节点，再保存父模板，列表显示停用版本6、10节点。
- 保存后CloudBase只读回读：全部10节点版本6；目标仅fields/updatedAt/version改变，另外9节点仅updatedAt/version改变。9个无关字段只整体后移5位、其余完全保留；品牌/型号稳定标识保留，旧SKU移除，8个联动字段全为单选。卡片修订2、流程入口、节点列表、描述和停用状态保持。完整2495组合还原摘要`ee23f9c98dddeb9ca4d6298c0c1e9b4daa84124cfedbcd3b1c2c8c2e2798b50d`与源导入摘要一致；父模板定义摘要已更新。
- 发布前主流程API1333/1333、解析器38/38、客户端485/485；同轮统计65/65、检索119/119、WXML4/4；源矩阵2495完整路径逐条通过服务端值校验。本次仅保存配置，没有重跑代码测试；云端回读为新鲜证据，完整原生交互及真机性能仍不能由自动化结果代替。

## 配置顺序

1. 自动化及独立审查通过后再发布兼容运行库、上传客户端。
2. 热重载结束后从模板管理重新打开目标模板和节点，避免使用已失去父页面上下文的空草稿。
3. 核对唯一目标、当前版本、已停用状态，确认卡片/条件/路由未引用拟删SKU；有引用时停止并取得具体调整授权。
4. 只读源表摘要未改变才使用ignored `qa/product-option-linkage-2026-09-18.json`。已审核有效2495组合、229型号组；排除行及缓存来源说明见设计。
5. 普通节点编辑页粘贴JSON，校验、查看摘要及试选；确认后应用草稿并保存节点，再保存整个模板。禁止直接修改template_nodes而不更新模板摘要。
6. 从已保存定义重新加载，核对版本/摘要、八字段单选、核心字段稳定标识及2495组合。其他节点、卡片、参与人、时限、凭证和路由不变；不擅自启用原停用模板。

## 验收矩阵

- 四类别逐级试选；相同品牌跨分类不串型号。
- 至少一条非笛卡尔积多属性路径不能选出不存在组合。
- 中间空属性列隐藏并保留原列身份；上级变更清除不合法后继。
- 仅单一选项也需明确选择；试选不改变草稿或写业务数据。
- 实际2495有效路径枚举集合与源集合一致，逐条服务端最终校验成功；实际数据仅在本地ignored验证，不记录明细。
- 合成测试覆盖规则损坏、访问器、数组与字节边界、模板复制、旧客户端丢规则拦截、等价重排统计兼容及CSV/分享/卡片/检索/识别消费者。
- 手机/Mac实际渲染与网络耗时须单独标识，不能用本机Node性能或自动化测试代替。

## 恢复约束

配置前发布失败即停止，不写半份映射。新规则已保存/使用后，不回滚到会丢弃规则的旧服务或客户端；模板恢复须走正式编辑保存和版本检查。发布前代码副本只供经评估的恢复，不是自动回滚授权。
