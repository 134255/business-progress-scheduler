# 定时任务可信来源修复实施计划

1. 在 `workflowReminder` 与 `calendarSync` 的计划入口测试中新增可信环境来源和伪造上下文矩阵，先确认现有实现失败。
2. 为两个处理器增加可注入的可信来源读取器，默认部署装配读取 `process.env.TRIGGER_SRC`。
3. 保留客户端身份拒绝、服务端时钟、固定批量和人工日历同步票据语义。
4. 运行聚焦测试和完整回归，检查事务与错误边界未变化。
5. 更新项目状态，记录真实 Timer 失败、根因、修复证据及仍待完成的 CloudBase 重新部署验收。
6. 清理本次只读 SDK 检查生成的临时目录，显式暂存本任务文件并创建本地提交；用户的 `project.config.json` 修改始终排除。
